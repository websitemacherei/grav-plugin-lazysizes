en:
  PLUGIN_LAZYSIZES:
    INCLUDE_JS: Inlcude lazysizes javascript into site
    INCLUDE_JS_HELP: When disabled, this plugin won't work until you include `lazysizes.js` somewhere else (e.g. your template)!
    IMAGE_WIDTHS: Image widths
    FALLBACK: Fallback
    FALLBACK_SEETINGS: Fallback Settings
    FILTER: Filter
    VALUE: Value


